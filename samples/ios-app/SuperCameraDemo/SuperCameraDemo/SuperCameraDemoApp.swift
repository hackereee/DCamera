import Foundation

// Library target entry point. For a real iOS app, this would be a SwiftUI @main App.
// Since we build with SPM as a library target, SwiftUI is not available here.
enum SuperCameraDemoApp {
    static let appName = "SuperCameraDemo"
}
